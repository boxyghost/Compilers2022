public class Hello {
  Hello h1;
  // int h2 = 3;
  public static /*beepboop*/ int HelloWorld(int x) { // brynn lets figure out how to make it check the return values
    /*String should be able to be string when we add inbuilt in types*/int sus;
    sus = "yo";
    /*String*/int b = "b";
    // b++;
    // b++;
    //wrong++; // this should throw an error; this is an error for method??? brynn lets check this
    h1++; // this is defined above
    goodbyeWorld();
    // int i;
    // i = 0;
    // int x = 0;
    // for(int x; i>0; i = i+1) {
    //   i = 2;
    // }
  }
  public static int goodbyeWorld(int y) {
    HelloWorld(3);
    Hello x = new Hello();
    int array1 = new int[10];
    int redefined;
    int redefined;
    int redefined2 = 2;
    int redefined2 = 3;
    int redefined3;
    int redefined3 = 2;
    // errorfunc(); // this should cause an error
    // ErrorObjConstructed1 eo; // should cause an error
    // ErrorObjConstructed eo = new ErrorObjConstructed(); // this is to give an error on first part, not cosntreuctor.
    // ErrorArrayObj array1 = new ErrorArrayObj[10]; // probably should check these more throughly. Can you help brynn?
  }

  public Hello() {
    int c1;
    int c2 = 6;
    c2 = c3; //should throw an error
  }
  // public ErrorObjConstructed() { // this will give an error becuase theres no class -- sort of counter intuitive with the tests but I belive if you have the other uncommented and this, it will get hung up on the top one, declaring the missing class and not even bother with the constructor. its just a big mess lol
  //   int c2 = 6;
  //   c2 = c3; //should throw an error
  // }

}
